import asyncio from scene_controller import SceneController async def demo_interaction(): """Demo de interaccion con la escena 3D""" print("=== Iniciando interaccion con VTTL ===") async with SceneController() as scene: print("? Conectado al servidor") # Limpiar escena print("\n1. Limpiando escena...") await scene.clear_scene() await asyncio.sleep(1) # Crear objetos print("\n2. Creando objetos 3D...") await scene.create_cube("cubo_rojo", [0, 0.5, 0], color=[1, 0, 0]) await scene.create_sphere("esfera_azul", [3, 0.5, 0], color=[0, 0, 1]) await scene.create_cylinder("cilindro_verde", [-3, 0.5, 0], color=[0, 1, 0]) await asyncio.sleep(1) # Posicionar camara print("\n3. Ajustando camara...") await scene.set_camera([5, 5, 5], target=[0, 0, 0]) await asyncio.sleep(1) # Animacion en circulo print("\n4. Animando objetos en circulo...") await scene.arrange_in_circle(["cubo_rojo", "esfera_azul", "cilindro_verde"], radius=2.5) await asyncio.sleep(2) # Tomar screenshot print("\n5. Tomando screenshot...") await scene.take_screenshot() print("\n? Demo completada!") print("Revisa la carpeta screenshots para ver el resultado") if __name__ == "__main__": asyncio.run(demo_interaction())
